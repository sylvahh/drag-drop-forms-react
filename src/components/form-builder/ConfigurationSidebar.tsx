import React from 'react';

const ConfigurationSidebar: React.FC = () => {
  // This component is now replaced by ConfigurationDrawer
  // Keeping it empty for backward compatibility
  return null;
};

export default ConfigurationSidebar;
