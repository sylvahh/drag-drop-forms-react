import * as React from "react";
import NewComplianceDialog from "./new-compliance-dialog";

export default React.memo(function ComplianceDialogs() {
	return (
		<React.Fragment>
			<NewComplianceDialog />
		</React.Fragment>
	);
});
